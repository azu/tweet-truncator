{
  "name": "tweet-truncate",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/azu/tweet-truncate.git"
  },
  "author": "azu",
  "email": "azuciao@gmail.com",
  "homepage": "https://github.com/azu/tweet-truncate",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/azu/tweet-truncate/issues"
  },
  "version": "1.0.1",
  "main": "lib/index.js",
  "files": [
    "lib/",
    "src/"
  ],
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "babel src --out-dir lib --source-maps",
    "watch": "babel src --out-dir lib --watch --source-maps",
    "prepublish": "npm run --if-present build",
    "test": "mocha && (cd example && npm it)"
  },
  "keywords": [
    "twitter",
    "truncate",
    "length"
  ],
  "description": "Truncate contents of tweet to 140 chars.",
  "devDependencies": {
    "babel-cli": "^6.3.17",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-preset-es2015": "^6.3.13",
    "espower-babel": "^4.0.1",
    "mocha": "^2.3.4",
    "power-assert": "^1.2.0"
  },
  "dependencies": {
    "object-assign": "^4.0.1",
    "twitter-text": "^1.13.2"
  }
}
